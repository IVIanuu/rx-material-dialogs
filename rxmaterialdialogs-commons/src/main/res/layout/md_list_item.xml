<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?selectableItemBackground"
    android:focusable="true"
    android:gravity="start|center_vertical"
    android:minHeight="@dimen/md_list_height"
    android:paddingTop="4dp"
    android:layout_marginBottom="4dp"
    android:paddingEnd="@dimen/md_dialog_frame_margin"
    android:paddingLeft="@dimen/md_dialog_frame_margin"
    android:paddingRight="@dimen/md_dialog_frame_margin"
    android:paddingStart="@dimen/md_dialog_frame_margin"
    tools:ignore="PrivateResource">

    <ImageView
        android:id="@android:id/icon"
        android:layout_width="@dimen/md_list_icon"
        android:layout_height="@dimen/md_list_icon"
        android:layout_alignParentLeft="true"
        android:layout_centerVertical="true"
        android:layout_marginEnd="@dimen/md_list_icon_margin"
        android:layout_marginRight="@dimen/md_list_icon_margin"
        android:background="@drawable/gray_circle"
        android:scaleType="fitXY"
        tools:background="#f5f5f5"
        tools:ignore="ContentDescription" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_alignParentRight="true"
        android:layout_toRightOf="@android:id/icon"
        android:orientation="vertical">

        <TextView
            android:id="@android:id/title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:textSize="@dimen/md_list_title_size"
            tools:text="Title" />

        <TextView
            android:id="@android:id/text1"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:textSize="@dimen/md_list_text_size"
            tools:text="Text" />

    </LinearLayout>

</RelativeLayout>